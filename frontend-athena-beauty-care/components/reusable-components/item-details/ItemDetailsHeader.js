//

export default function ItemDetailsHeader({ itemHeaders, index }) {

    return (
       
        // Styles are in globals.css
        <h4 className="itemDetails_content_header">{itemHeaders[index]}</h4>
                            
    
    );

}