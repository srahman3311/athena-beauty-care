import { findTimeSlots } from "../lib/find-time-slots-copy/findTimeSlots";

export default function Test () {

    

    return (
        <div className="">
            <button onClick = {findTimeSlots}>Test</button>
        </div>
    );
}